spring:
  application:
    name: micro-weather-config-client

  cloud:
      config:
        profile: dev
        # 指向config server
        uri: http://localhost:8091/

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/



server:
  port: 8092

